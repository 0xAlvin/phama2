/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: [
      'images.unsplash.com'
    ],
  },
  // Add any other configuration you had in the TS file
};

module.exports = nextConfig;
